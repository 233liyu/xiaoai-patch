diff --git a/etc/init.d/bluetooth b/etc/init.d/bluetooth
index 6e1f466..bd181ca 100755
--- a/etc/init.d/bluetooth
+++ b/etc/init.d/bluetooth
@@ -11,8 +11,7 @@ DEBUG=`cat /data/bt/debug`
 fi
 PROG1=/usr/bin/bluealsa
 PROG2=/usr/bin/bluealsa-aplay
-PROG3=/usr/bin/bluez_mibt_classical
-PROG5=/usr/bin/mico_ble_service
+ALSA_MIXER=bluetooth
 
 function bluealsa_start() {
 	procd_open_instance
@@ -35,35 +34,16 @@ function bluealsa_start() {
 	# procd_close_instance
 }
 
-function bluez_mibt_classical_start() {
-	procd_open_instance
-	procd_set_param command "$PROG3"
-	procd_append_param respawn 3600
-	procd_append_param respawn 5
-	procd_append_param respawn 5
-	procd_close_instance
-}
-
-# function bluez_mible_start() {
-# 	procd_open_instance
-# 	procd_set_param command "$PROG5"
-# 	procd_append_param respawn 3600
-# 	procd_append_param respawn 5
-# 	procd_append_param respawn 5
-# 	procd_close_instance
-# }
-
 function bluez_start() {
 		echo "Start	bluealsa"
 		bluealsa_start
-		sleep 2
-		echo "Start	bluez_mibt_classical"
-		bluez_mibt_classical_start
-		[ ! -f "/data/status/config_done" ] && {
-			# echo "mibt Start BLE"
-			# bluez_mibt_ble_start
-			/etc/init.d/bluetooth_ble start
-		}
+		# sleep 2
+		return 0
+		#[ ! -f "/data/status/config_done" ] && {
+		#	# echo "mibt Start BLE"
+		#	# bluez_mibt_ble_start
+		#	/etc/init.d/bluetooth_ble start
+		#}
 }
 
 start_service() {
diff --git a/etc/init.d/bluetoothd b/etc/init.d/bluetoothd
index f2b62e0..72ea29a 100755
--- a/etc/init.d/bluetoothd
+++ b/etc/init.d/bluetoothd
@@ -21,7 +21,7 @@ function bluetoothd_start() {
         cat /proc/kmsg >/tmp/dmsg.log &
     else
         procd_open_instance
-        procd_set_param command "$PROG" -n
+        procd_set_param command "$PROG" -n -f $conf_dir/bluetooth/main.conf
         procd_set_param respawn 3600 5 0
         procd_close_instance
     fi
@@ -39,7 +39,7 @@ start_service() {
         rtk_hciattach -n -s 115200 ttyS1 rtk_h5 > /dev/null 2>&1 &
     fi
     sleep 2
-    btmgmt power on
+    timeout 4 btmgmt power on
 
     if [ ! -d $conf_dir/bluetooth ];then
         /bin/mkdir -p $conf_dir/bluetooth
